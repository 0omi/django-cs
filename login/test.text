
class Login(models.Model):
    username = models.CharField(max_length=50)
    password = models.CharField(max_length=50)

    def __str__(self):
        return self.name 
في صفحه الموديل 



from .models import Login # بجرب احذفها بعدين 
في صفحه الفيو 